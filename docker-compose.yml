version: "3"
services:
  react-app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./src/main/frontend/src:/app/src
    environment:
      - REACT_APP_NAME=react-tutorial
      - CHOKIDAR_USEPOLLING=true
    #env_file:
    #	- ./.env